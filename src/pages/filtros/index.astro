---
import WebsiteLayout from "@/layouts/websiteLayout.astro";
import SEO from "@/components/shared/SEO.astro";
import FiltersContent from "@/components/shared/FiltersContent.astro";
import { generateBreadcrumbSchema } from "@/lib/seo-utils";

export const prerender = false;

const siteUrl = Astro.site?.href || Astro.url.origin;
const breadcrumbSchema = generateBreadcrumbSchema([
  { name: 'Inicio', url: siteUrl },
  { name: 'Filtros de Aire', url: `${siteUrl}/filtros` }
]);
---

<WebsiteLayout
  title="Filtros de Aire"
  seoTitle="Filtros de Aire - Catálogo Star Filters"
  description="Explora nuestro catálogo completo de filtros de aire. Productos de alta calidad con envío a toda la República Mexicana."
  keywords={[
    'filtros de aire',
    'comprar filtros de aire',
    'filtros industriales',
    'tienda filtros México'
  ]}
>
  <SEO
    slot="seo"
    title="Filtros de Aire"
    description="Explora nuestro catálogo completo de filtros de aire. Productos de alta calidad con envío a toda la República Mexicana."
    ogType="website"
    keywords={[
      'filtros de aire',
      'comprar filtros de aire',
      'filtros industriales'
    ]}
  />

  <!-- Breadcrumb Schema -->
  <script slot="seo" type="application/ld+json" set:html={JSON.stringify(breadcrumbSchema)} is:inline />

  <FiltersContent />
</WebsiteLayout>
