---
import WebsiteLayout from "@/layouts/websiteLayout.astro";
import { ZONA_HORARIA } from "@/lib/timezone";
import SEO from "@/components/shared/SEO.astro";
import { getCaseStudyBySlug, getActiveCaseStudies } from "@/lib/database";
import { generateBreadcrumbSchema } from "@/lib/seo-utils";
import { getHeroImageUrl, getHeroPlaceholderUrl } from "@/lib/image-utils";
import { buildCaseStudySections } from "@/lib/case-study-content";
import { getWhatsAppNumber } from "@/lib/site-settings-service";

export const prerender = false;

const { slug } = Astro.params;
if (!slug) return Astro.redirect('/en/success-cases');

const caseStudy = await getCaseStudyBySlug(slug);
if (!caseStudy) return Astro.redirect('/en/success-cases');

const title = caseStudy.title_en || caseStudy.title;
const excerpt = caseStudy.excerpt_en || caseStudy.excerpt;
const challenge = caseStudy.challenge_en || caseStudy.challenge;
const solution = caseStudy.solution_en || caseStudy.solution;
const results = caseStudy.results_en || caseStudy.results;
const industry = caseStudy.industry_en || caseStudy.industry;

const related = (await getActiveCaseStudies())
  .filter((c) => c.slug !== slug && c.industry === caseStudy.industry)
  .slice(0, 3);

const siteUrl = Astro.site?.href || Astro.url.origin;
const breadcrumbSchema = generateBreadcrumbSchema([
  { name: 'Home', url: siteUrl },
  { name: 'Success Cases', url: `${siteUrl}/en/success-cases` },
  { name: title, url: `${siteUrl}/en/success-cases/${caseStudy.slug}` },
]);

const getTagsArray = (tags?: string) =>
  tags ? tags.split(',').map((t) => t.trim()).filter(Boolean) : [];
const tags = getTagsArray(caseStudy.tags);

const formatDate = (date: Date) =>
  new Intl.DateTimeFormat('en-US', {
    timeZone: ZONA_HORARIA, year: 'numeric', month: 'long' }).format(new Date(date));

// Challenge / solution / results, numbered over what the case actually has:
// a case with no challenge starts at 01, not 02.
const sections = buildCaseStudySections([
  { label: 'The challenge', body: challenge },
  { label: 'The solution', body: solution },
  { label: 'The results', body: results },
]);

// The gallery continues the same numbering as the text sections.
const galleryNumber = String(sections.length + 1).padStart(2, '0');

// Project fact sheet: only the data the case actually has.
const fichaRows = [
  { label: 'Industry', value: industry },
  { label: 'Client', value: caseStudy.client_name },
  { label: 'Date', value: formatDate(caseStudy.created_at) },
].filter((row): row is { label: string; value: string } => Boolean(row.value));

const sidebarWhatsappNumber = (await getWhatsAppNumber()) || '523322569541';
const sidebarWhatsappUrl = `https://wa.me/${sidebarWhatsappNumber}?text=${encodeURIComponent(
  `Hello, I read the "${title}" case study and I would like help with a similar filtration challenge.`
)}`;
---

<WebsiteLayout
  title={title}
  seoTitle={`${title} | Success Cases | Star Filters`}
  description={excerpt || `Star Filters success case in ${industry}: challenge, solution, and results.`}
  keywords={[...tags, 'Star Filters', industry, 'industrial filtration']}
  ogImage={caseStudy.featured_image}
>
  <SEO
    slot="seo"
    title={`${title} | Star Filters`}
    description={excerpt || `Industrial filtration success case in ${industry}.`}
    ogType="article"
    ogImage={caseStudy.featured_image}
    keywords={[...tags, 'Star Filters', industry]}
  />
  <script slot="seo" type="application/ld+json" set:html={JSON.stringify(breadcrumbSchema)} />

  <!-- "Technical dossier" layout: one container width from top to bottom
       (the header used max-w-6xl and the body max-w-4xl, so the page drifted
       out of alignment mid-read), one accent colour (the brand blue, instead of
       the red/blue/green circles that belonged to no palette) and a sticky
       project fact sheet on the right with the hard data.
       pt-12: the navbar is absolute and 104px tall, and <main> only adds 64px. -->
  <article class="bg-background pt-12">
    <div class="mx-auto max-w-6xl px-6 pt-6">

      <nav class="mb-6 flex flex-wrap items-center gap-2 text-sm text-muted-foreground">
        <a href="/en" class="hover:text-foreground transition-colors">Home</a>
        <svg class="h-4 w-4 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
        </svg>
        <a href="/en/success-cases" class="hover:text-foreground transition-colors">Success Cases</a>
        <svg class="h-4 w-4 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
        </svg>
        <span class="text-foreground font-medium">{title}</span>
      </nav>

      <p class="text-sm font-semibold uppercase tracking-widest text-primary">
        Success case · {industry}
      </p>
      <h1 class="mt-2 max-w-4xl text-4xl font-bold tracking-tight text-foreground sm:text-5xl">
        {title}
      </h1>
      {excerpt && (
        <p class="mt-5 max-w-3xl text-lg leading-relaxed text-muted-foreground">{excerpt}</p>
      )}

      {caseStudy.featured_image ? (
        <!-- The background is the ~1 KB blurred thumbnail: it shows instantly and the
             full photo covers it on load, with no JavaScript involved. -->
        <figure
          class="mt-9 overflow-hidden rounded-2xl border border-border bg-muted"
          style={getHeroPlaceholderUrl(caseStudy.featured_image)
            ? `background-image:url('${getHeroPlaceholderUrl(caseStudy.featured_image)}');background-size:cover;background-position:center;`
            : undefined}
        >
          <img
            src={getHeroImageUrl(caseStudy.featured_image)}
            alt={title}
            loading="eager"
            fetchpriority="high"
            decoding="async"
            class="block h-64 w-full object-cover sm:h-[420px]"
          />
        </figure>
      ) : (
        <div class="mt-9 h-64 rounded-2xl bg-gradient-to-br from-primary/10 to-primary/5 sm:h-[420px]"></div>
      )}

      <div class="mt-14 grid items-start gap-12 lg:grid-cols-[1fr_20rem] lg:gap-14">

        <!-- Reading column -->
        <div>
          {sections.map((section) => (
            <section class="mb-12 last:mb-0">
              <div class="flex items-baseline gap-3 border-b border-border pb-3">
                <span class="text-sm font-bold tracking-wider text-primary">{section.number}</span>
                <h2 class="text-2xl font-semibold tracking-tight text-foreground">{section.label}</h2>
              </div>
              <div class="mt-5 space-y-4">
                {section.blocks.map((block) => (
                  block.type === 'paragraph' ? (
                    <p class="text-lg leading-relaxed text-muted-foreground">{block.text}</p>
                  ) : (
                    <ul class="flex flex-col gap-4">
                      {block.items.map((item) => (
                        <li class="flex items-start gap-3">
                          <span class="mt-2.5 h-1.5 w-1.5 flex-shrink-0 rounded-full bg-primary"></span>
                          <span class="text-lg leading-relaxed text-muted-foreground">{item}</span>
                        </li>
                      ))}
                    </ul>
                  )
                ))}
              </div>
            </section>
          ))}

          {caseStudy.gallery_images && caseStudy.gallery_images.length > 0 && (
            <section class="mt-12">
              <div class="flex items-baseline gap-3 border-b border-border pb-3">
                <span class="text-sm font-bold tracking-wider text-primary">{galleryNumber}</span>
                <h2 class="text-2xl font-semibold tracking-tight text-foreground">Project gallery</h2>
              </div>
              <div class="mt-5 grid grid-cols-2 gap-3 sm:grid-cols-3">
                {caseStudy.gallery_images.map((url, i) => (
                  <button
                    type="button"
                    class="gallery-thumb group relative aspect-square overflow-hidden rounded-xl focus:outline-none focus:ring-2 focus:ring-primary"
                    data-src={url}
                    data-index={i}
                    aria-label={`View image ${i + 1}`}
                  >
                    <img
                      src={url}
                      alt={`${title} – imagen ${i + 1}`}
                      class="h-full w-full object-cover transition-transform duration-300 group-hover:scale-105"
                      loading="lazy"
                    />
                    <div class="absolute inset-0 bg-black/0 group-hover:bg-black/20 transition-colors flex items-center justify-center">
                      <svg class="h-8 w-8 text-white opacity-0 group-hover:opacity-100 transition-opacity" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0zM10 7v3m0 0v3m0-3h3m-3 0H7"/>
                      </svg>
                    </div>
                  </button>
                ))}
              </div>
            </section>
          )}
        </div>

        <!-- Project fact sheet: los datos duros, sin tener que leer el cuerpo.
             Sticky so it stays in view while reading. -->
        <aside class="lg:sticky lg:top-28">
          <div class="overflow-hidden rounded-xl border border-border shadow-sm">
            <div class="bg-primary px-5 py-4">
              <p class="text-xs font-semibold uppercase tracking-widest text-primary-foreground">Project fact sheet</p>
            </div>
            {fichaRows.map((row) => (
              <div class="flex justify-between gap-4 border-b border-border px-5 py-3.5">
                <span class="text-sm text-muted-foreground">{row.label}</span>
                <span class="text-right text-sm font-semibold text-foreground">{row.value}</span>
              </div>
            ))}
            {tags.length > 0 && (
              <div class="px-5 pb-5 pt-4">
                <p class="mb-2.5 text-sm text-muted-foreground">Services</p>
                <div class="flex flex-wrap gap-1.5">
                  {tags.map((tag) => (
                    <span class="inline-flex items-center rounded-md bg-muted px-2.5 py-1 text-xs text-muted-foreground">{tag}</span>
                  ))}
                </div>
              </div>
            )}
          </div>

          <div class="mt-4 rounded-xl border border-border p-5">
            <p class="text-lg font-semibold tracking-tight text-foreground">Facing a similar challenge?</p>
            <p class="mt-2 text-sm leading-relaxed text-muted-foreground">
              Our engineers review your case and propose the filtration solution for your process.
            </p>
            <a
              href={sidebarWhatsappUrl}
              target="_blank"
              rel="noopener noreferrer"
              class="mt-4 flex items-center justify-center gap-2 rounded-md bg-[#25D366] px-4 py-3 text-sm font-semibold text-white transition hover:bg-[#1ebe5d]"
            >
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="h-[18px] w-[18px] flex-shrink-0">
                <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884"/>
              </svg>
              Contact via WhatsApp
            </a>
          </div>
        </aside>
      </div>

      <!-- Lightbox -->
      <div id="lightbox" class="hidden fixed inset-0 z-50 flex items-center justify-center bg-black/90 p-4" role="dialog" aria-modal="true">
        <button id="lb-prev" class="absolute left-4 top-1/2 -translate-y-1/2 text-white hover:text-gray-300 transition-colors p-2" aria-label="Previous">
          <svg class="h-8 w-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
          </svg>
        </button>
        <img id="lb-img" src="" alt="" class="max-h-[90vh] max-w-full rounded-lg object-contain" />
        <button id="lb-next" class="absolute right-4 top-1/2 -translate-y-1/2 text-white hover:text-gray-300 transition-colors p-2" aria-label="Next">
          <svg class="h-8 w-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
          </svg>
        </button>
        <button id="lb-close" class="absolute top-4 right-4 text-white hover:text-gray-300 transition-colors p-2" aria-label="Close">
          <svg class="h-7 w-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
          </svg>
        </button>
        <span id="lb-counter" class="absolute bottom-4 left-1/2 -translate-x-1/2 text-sm text-white/70"></span>
      </div>

      {related.length > 0 && (
        <div class="mt-16">
          <h3 class="mb-6 text-xl font-semibold tracking-tight text-foreground">More cases in {industry}</h3>
          <div class="grid grid-cols-1 gap-5 sm:grid-cols-3">
            {related.map((r) => (
              <a
                href={`/en/success-cases/${r.slug}`}
                class="group overflow-hidden rounded-xl border border-border transition-all hover:border-primary/50 hover:shadow-sm"
              >
                {r.featured_image && (
                  <div class="h-40 overflow-hidden">
                    <img src={r.featured_image} alt={r.title_en || r.title} class="h-full w-full object-cover transition-transform group-hover:scale-105" loading="lazy" />
                  </div>
                )}
                <div class="p-4">
                  <p class="text-sm font-semibold leading-snug text-foreground transition-colors group-hover:text-primary line-clamp-2">{r.title_en || r.title}</p>
                </div>
              </a>
            ))}
          </div>
        </div>
      )}

      <div class="mt-10 section-pb">
        <a href="/en/success-cases" class="inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground transition-colors">
          <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
          </svg>
          View all success cases
        </a>
      </div>
    </div>
  </article>

  <script>
    const thumbs = Array.from(document.querySelectorAll<HTMLButtonElement>('.gallery-thumb'));
    const lightbox = document.getElementById('lightbox')!;
    const lbImg = document.getElementById('lb-img') as HTMLImageElement;
    const lbCounter = document.getElementById('lb-counter')!;
    let current = 0;

    const srcs = thumbs.map((t) => t.dataset.src!);

    function show(index: number) {
      current = (index + srcs.length) % srcs.length;
      lbImg.src = srcs[current];
      lbCounter.textContent = `${current + 1} / ${srcs.length}`;
      lightbox.classList.remove('hidden');
      document.body.style.overflow = 'hidden';
    }

    function close() {
      lightbox.classList.add('hidden');
      document.body.style.overflow = '';
    }

    thumbs.forEach((btn, i) => btn.addEventListener('click', () => show(i)));
    document.getElementById('lb-close')!.addEventListener('click', close);
    document.getElementById('lb-prev')!.addEventListener('click', () => show(current - 1));
    document.getElementById('lb-next')!.addEventListener('click', () => show(current + 1));
    lightbox.addEventListener('click', (e) => { if (e.target === lightbox) close(); });
    document.addEventListener('keydown', (e) => {
      if (lightbox.classList.contains('hidden')) return;
      if (e.key === 'Escape') close();
      if (e.key === 'ArrowLeft') show(current - 1);
      if (e.key === 'ArrowRight') show(current + 1);
    });
  </script>

</WebsiteLayout>
